return {
  {
    "RRethy/nvim-base16",
    lazy = false,
    priority = 1000,
    config = function()
      -- Base16 will be set via theme-sync module
      -- This just ensures the plugin is loaded
    end,
  },
} 